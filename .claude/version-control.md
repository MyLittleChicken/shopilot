# 버전 관리

- 커밋은 의미 단위로 나누고, 컨벤션 메시지를 쓴다(`feat:`, `fix:`, `chore:`, `docs:` 등).
- 기본 브랜치는 `main`. 작업 브랜치(`feat/…` 등)에서 작업하고, 구현·테스트가 끝나면 PR로 합친다. `main`에 직접 커밋하지 않는다.
- 빌드 산출물과 시크릿은 커밋하지 않는다. 무시 대상은 `.gitignore`가 단일 기준이다.
- 광역 `git add .`/`-A` 대신 파일을 명시적으로 스테이징하고, 커밋 전 시크릿·금지 파일이 섞이지 않았는지 확인한다.
